/**
 * Shared outer layout for main page sections so stacked sections share the same
 * vertical rhythm (gap at seams = top + bottom padding) and horizontal inset.
 */
export const SECTION_SHELL_BASE = 'min-h-screen px-4 py-20 md:px-8'
